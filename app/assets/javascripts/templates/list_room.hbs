
  <div class="room_banner">
    <h3>Active Rooms</h3>
  </div>
  <div class = "room-container">
      <ul class="row clearfix">
          {{#each studio}}
          <div id="{{name}}" class="studio column full">
            <p id="{{name}}">
              Name {{name}}
              <i class="fi-people"> {{listeners}} </i>
               <i class="fi-location"> {{distance}} </i>
            </p>
          </div>
          {{/each}}
      </ul>
  </div>

